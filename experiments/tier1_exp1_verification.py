"""Tier 1 · Exp 1 — verification (confusion matrix per grader; headline vs A2).

GATED — DO NOT BUILD until Tier 0 passes AND a human authorizes Tier 1 (§0.2, §0.5, §6).
This tripwire exists so no agent silently crosses the gate. Intended contents when unblocked:
per-grader confusion matrix; hacks-caught-beyond-A2; spurious-catch; footprint accuracy as a
region/signal match (review note 3); FP on valid alternatives.
"""
raise NotImplementedError(
    "GATED: Tier 1 is built only after Tier 0 PASSES and a human authorizes it "
    "(build plan §0.2, §3 Phase D, §6). Do not implement from here."
)
