# Tier 2 — RL arms (GATED)

**DO NOT BUILD.** Built only if **Tier 1 PASSES** *and* RL feasibility = yes (spec §12),
*and* a human authorizes it (build plan §0.2, §0.5, §3 Phase E, §6).

When unblocked, this holds the short GRPO run per grader arm on **contamination-controlled**
tasks, with stability measured on a **held-out split** (review note 2).

**Pre-registered positive control (spec tweak 2):** Tier 2 is interpretable *only if the A2
arm demonstrably develops reward-hacking* (the widening gap). If it does not, the result is
**INCONCLUSIVE** — un-gameability stays *unproven under pressure*, **not** GREEN.

This directory is intentionally empty of implementation. Leaving it unbuilt is correct.
