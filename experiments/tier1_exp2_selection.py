"""Tier 1 · Exp 2 — selection (best-of-N).

GATED — DO NOT BUILD until Tier 0 passes AND a human authorizes Tier 1 (§0.2, §0.5, §6).
Intended contents when unblocked: best-of-N selection; true-correct@1 delta vs A2;
selected-hack rate. Note (revisions Change 1): best-of-N on a frozen model tests
selector ACCURACY, not un-gameability — that is Tier 2's job.
"""
raise NotImplementedError(
    "GATED: Tier 1 is built only after Tier 0 PASSES and a human authorizes it "
    "(build plan §0.2, §3 Phase D, §6). Do not implement from here."
)
